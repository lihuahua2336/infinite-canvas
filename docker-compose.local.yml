services:
  app:
    image: infinite-canvas:local
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      APP_PUBLIC_URL: ${APP_PUBLIC_URL:-}
    ports:
      - "3003:3000"
    restart: unless-stopped
