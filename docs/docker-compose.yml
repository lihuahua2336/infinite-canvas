services:
  docs:
    image: ghcr.io/lihuahua2336/infinite-canvas-docs:latest
    env_file:
      - path: ../.env.production.local
        required: false
    ports:
      - "3001:3000"
    restart: unless-stopped
