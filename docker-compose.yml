services:
  app:
    image: ghcr.io/lihuahua2336/infinite-canvas:latest
    container_name: infinite-canvas
    env_file:
      - .env
    ports:
      - "${APP_PORT:-3000}:3000"
    restart: unless-stopped
